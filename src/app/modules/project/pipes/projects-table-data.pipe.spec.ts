import { ProjectsTableDataPipe } from './projects-table-data.pipe';

describe('ProjectsTableDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectsTableDataPipe();
    expect(pipe).toBeTruthy();
  });
});
